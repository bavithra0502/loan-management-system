export const environment = {
  production: false,
  // Base URL of the ASP.NET Core Web API (LoanManagementAPI).
  // Update the port to match whatever `dotnet run` prints for your machine.
   apiUrl: 'https://localhost:44394/api/'
};
